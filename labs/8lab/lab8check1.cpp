#include <iostream>

/*
int recurse (int row, int col) {
    if (row == 0 || col == 0) {
        return 1;
    }
    return recurse(row - 1, col) + recurse(row, col - 1);    
}

int main() {
    int test = recurse(5,5);
    std::cout << test << std::endl;
}
*/

